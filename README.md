# Device Info App

A simple Flutter project that displays your device model name and operating system version at the center of the screen.

## Features

- Retrieves device model name
- Displays the operating system version
- Uses the [device_info_plus] package to fetch device information